from django.apps import AppConfig
"""
Module for configuring the 'trav' app.
"""

class TravConfig(AppConfig):
    """
    Configuration class for the 'trav' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'trav'
